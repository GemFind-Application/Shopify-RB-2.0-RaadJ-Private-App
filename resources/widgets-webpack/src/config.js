import { CONST } from "./black-box/constants";
const { TEST_BAR } = CONST;
export const config = {
    [TEST_BAR]: {
        target: "#MainContent",
        className: "ring-builder-advance-test-q78er",
        styles: {
            width: "100%",
        },
    },
};
